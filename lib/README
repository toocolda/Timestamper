
This project vendors third-party libraries in this directory to keep builds reproducible.

Current repo-local library:
- TinyGPSPlus: copied from PlatformIO libdeps and patched locally to accept `GN` talker IDs.

Framework-provided libraries are not vendored here:
- Wire
- EEPROM

Those two come from the selected Arduino framework package for the PlatformIO environment.
